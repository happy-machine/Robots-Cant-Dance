
      <button onClick="window.location='<%=new_room_path%>';" id="new_room" class="_button _button-5">New Room</button>
      <p class="news_text">** NEWS: This week MEGADIV has uploaded a demo on how to troll, we recommend checking it out! HERE **</p>
     <div id="form_position"><div id="back_plate" class="back_plate">
    <% @rooms.each do |room| %>
    <div>
     <div id="room_bar2" class="room_bar2"><%= link_to '', room %>
        <%= link_to room.name_capitalised, room_path(room) %>
       <div class="tools">
      <a href="<%=edit_room_path(room)%>"> <i class="fas fa-pencil-alt"></i></a>
       <%= link_to '', room, method: :delete, data: { confirm: 'Are you sure?' },class:'far fa-trash-alt' %>
     </div>
     </div>
      <div id="room_bar1" class="room_bar1"></div></div>
    <% end %>
       </div>
       </div>
